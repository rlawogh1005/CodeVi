server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API Proxy configuration
    location /api/ {
        # Proxy to backend service using the Docker service name
        # Trailing slash ensures /api/foo -> /foo on backend
        proxy_pass http://codevi-backend:13000/;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Extended timeouts for long-running requests
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }

    # Angular App (SPA) Support
    location / {
        try_files $uri $uri/ /index.html;
    }
}